name: Deploy AgendaMed API to GCP (Desativado - Migrado para Vercel)

# Workflow desativado temporariamente após migração da VM GCP para a Vercel
# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'api/**'
#       - '.github/workflows/deploy-agendamed-api.yml'
#   workflow_dispatch:

jobs:
  disabled-job:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Desativado
        run: echo "Deploy para VM GCP desativado. A API backend agora roda na Vercel."
